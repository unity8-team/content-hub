#!/usr/bin/make -f
# -*- makefile -*-

export DPKG_GENSYMBOLS_CHECK_LEVEL=4

override_dh_auto_test:
	dbus-test-runner -t make -p "-C" -p "build/tests/acceptance-tests" -p test

%:
	dh $@ --with migrations,click --fail-missing -- -B build
